import { StringToBrPipe } from './string-to-br.pipe';

describe('StringToBrPipe', () => {
  it('create an instance', () => {
    const pipe = new StringToBrPipe();
    expect(pipe).toBeTruthy();
  });
});
